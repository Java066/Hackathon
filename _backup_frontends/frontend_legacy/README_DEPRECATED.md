# Deprecated frontend assets

This folder contains the previous static frontend implementation.

The active React dashboard frontend now lives in `../react-dashboard/`.
These files are kept intentionally for compatibility/reference and should not be deleted blindly.
